import 'package:flutter/material.dart';

class TransactScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transact'),
      ),
      body: Center(
        child: Text('Transact Section Content'),
      ),
    );
  }
}